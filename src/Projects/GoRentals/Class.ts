import {
    IEntitySchemaConfig,
    //  EPropertyTypes 
} from '../../types';

const Class: IEntitySchemaConfig = {
    entity: 'class',
    schema_version: 'v4',
    properties: [
        // attributes currently not used - master paul
    ],
};

export default Class;
