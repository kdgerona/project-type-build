import { IEntitySchemaConfig, EPropertyTypes } from '../../types';

const Class: IEntitySchemaConfig = {
    entity: 'class',
    schema_version: 'v4',
    properties: [
        // 3 months ago pa na use - master paul empty pd ig query
        // attributes currently not used

    ],
};

export default Class;
