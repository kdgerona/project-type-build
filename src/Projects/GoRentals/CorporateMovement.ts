import { IEntitySchemaConfig } from '../../types';

const  CorporateMovement: IEntitySchemaConfig = {
  entity: 'corporate_movement',
  schema_version: 'v4',
  properties: []
};

export default  CorporateMovement;
