import {
    IEntitySchemaConfig,
    //  EPropertyTypes 
} from '../../types';

const Trim: IEntitySchemaConfig = {
    entity: 'trim',
    schema_version: 'v4',
    properties: [
        // attributes currently not used
    ],
};

export default Trim;
