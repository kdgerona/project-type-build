import { IEntitySchemaConfig } from '../../types';

const State: IEntitySchemaConfig = {
  entity: 'state',
  schema_version: 'v4',
  properties: [], //devs have no idea what are the attribute of this entity
};

export default State;
