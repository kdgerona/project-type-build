import { IEntitySchemaConfig } from '../../types';

const  CounterMovement: IEntitySchemaConfig = {
  entity: 'counter_movement',
  schema_version: 'v4',
  properties: []
};

export default  CounterMovement;
