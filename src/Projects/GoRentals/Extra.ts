import {
  IEntitySchemaConfig,
  // EPropertyTypes
} from '../../types';

const Extra: IEntitySchemaConfig = {
  entity: 'extra',
  schema_version: 'v4',
  properties: [
    // attribute property currently not used
  ],
};

export default Extra;
