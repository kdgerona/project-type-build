import {
  IEntitySchemaConfig,
  // EPropertyTypes
} from '../../types';

const Color: IEntitySchemaConfig = {
  entity: 'color',
  schema_version: 'v4',
  properties: [
    // attribute property currently not used
  ],
};

export default Color;
